# Network Configuration
NETWORK=eip155:421614  # eip155:42161 (Arbitrum One) or eip155:421614 (Arbitrum Sepolia)
ARBITRUM_RPC_URL=https://sepolia-rollup.arbitrum.io/rpc
MERCHANT_PRIVATE_KEY=0x...  # Merchant wallet that receives payments (after facilitator fees)

# x402 External Facilitator Integration
# IMPORTANT: You must register as a merchant with the facilitator operator first!
# The facilitator will provide you with:
#   - MERCHANT_API_KEY: Your unique API key for authenticated settlement requests
#   - FACILITATOR_URL: The facilitator service URL
# See README.md for merchant registration instructions.
FACILITATOR_URL=https://your-facilitator-url.com  # External x402 facilitator service URL
MERCHANT_API_KEY=your-merchant-api-key  # API key provided by facilitator operator (required for /settle)

# Optional: Quote Service (if using separate quote service)
QUOTE_SERVICE_URL=http://localhost:3001

# USDC Token on Arbitrum Sepolia
USDC_ADDRESS=0x...  # Use TestUSDC address from x402-service deployment

# Ollama Configuration
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=llama3.1:8b

# Metering Configuration
PRICE_PER_MESSAGE_MICRO_USDC=20000   # 0.02 USDC per message (0.10 USDC per 5 messages)
DAILY_CAP_MICRO_USDC=2000000         # 2 USDC daily cap (100 messages max)
BATCH_THRESHOLD_MESSAGES=5           # Settle every 5 messages (0.10 USDC + fees per batch)
BATCH_TIMEOUT_SECONDS=3600           # Or settle after 1 hour

# Server Configuration
PORT=3003
NODE_ENV=development
