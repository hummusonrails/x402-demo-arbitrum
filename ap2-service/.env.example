# Arbitrum One Configuration (Mainnet)
ARBITRUM_RPC_URL=https://arb1.arbitrum.io/rpc
MERCHANT_PRIVATE_KEY=0x...  # Merchant wallet that receives payments (after facilitator fees)

# x402 External Facilitator Integration
FACILITATOR_URL=https://your-facilitator-url.com  # External x402 facilitator service URL
FACILITATOR_ADDRESS=0x...  # Facilitator's wallet address (receives payments first)
MERCHANT_API_KEY=your-merchant-api-key  # API key provided by facilitator operator

# Optional: Quote Service (if using separate quote service)
QUOTE_SERVICE_URL=http://localhost:3001

# USDC Token on Arbitrum One
USDC_ADDRESS=0xaf88d065e77c8cC2239327C5EDb3A432268e5831  # Official USDC contract address on Arbitrum One

# Ollama Configuration
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=llama3.1:8b

# Metering Configuration
PRICE_PER_MESSAGE_MICRO_USDC=20000   # 0.02 USDC per message (0.10 USDC per 5 messages)
DAILY_CAP_MICRO_USDC=2000000         # 2 USDC daily cap (100 messages max)
BATCH_THRESHOLD_MESSAGES=5           # Settle every 5 messages (0.10 USDC + fees per batch)
BATCH_TIMEOUT_SECONDS=3600           # Or settle after 1 hour

# Server Configuration
PORT=3003
NODE_ENV=development
